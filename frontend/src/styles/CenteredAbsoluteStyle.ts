import styled from 'styled-components';

const CenteredAbsoluteStyle = styled.div`
  position: absolute;
  margin-left: auto;
  margin-right: auto;
  left: 0;
  right: 0;
  width: 100px;
  text-align: center;
`;

export default CenteredAbsoluteStyle;
